name: "No thanks action"
description: 'Say "no thanks" to new PRs'
author: "whyucare"

inputs:
  GITHUB_TOKEN:
    description: "Github token"
    required: true
  TENOR_TOKEN:
    description: "Tenor API token"
    required: true

runs:
  using: "node12"
  main: "dist/index.js"
